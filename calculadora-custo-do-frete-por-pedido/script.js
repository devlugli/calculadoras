function moeda(valor) { return Number(valor).toLocaleString("pt-BR", { style: "currency", currency: "BRL" }); }
function calcular() { const frete = parseFloat(document.getElementById("freteMensal").value) || 0; const pedidos = parseInt(document.getElementById("pedidosMes").value, 10) || 0; const ticket = parseFloat(document.getElementById("ticketMedio").value) || 0; const porPedido = pedidos > 0 ? frete / pedidos : 0; const impacto = ticket > 0 ? (porPedido / ticket) * 100 : 0; document.getElementById("resumoTopoValor").innerText = moeda(porPedido); document.getElementById("resumoTopoTexto").innerText = `O frete representa aproximadamente ${impacto.toFixed(2)}% do ticket medio informado.`; } window.onload = function () { document.querySelectorAll("input").forEach(function (c) { c.addEventListener("input", calcular); }); calcular(); };
