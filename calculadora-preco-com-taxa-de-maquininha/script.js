function moeda(valor) { return Number(valor).toLocaleString("pt-BR", { style: "currency", currency: "BRL" }); }
function calcular() { const liquido = parseFloat(document.getElementById("valorLiquido").value) || 0; const taxa = (parseFloat(document.getElementById("taxaMaquininha").value) || 0) / 100; const bruto = taxa >= 1 ? 0 : liquido / (1 - taxa); document.getElementById("resumoTopoValor").innerText = moeda(bruto); document.getElementById("resumoTopoTexto").innerText = `Para receber ${moeda(liquido)}, o preco estimado com a taxa informada e ${moeda(bruto)}.`; } window.onload = function () { document.querySelectorAll("input").forEach(function (c) { c.addEventListener("input", calcular); }); calcular(); };
