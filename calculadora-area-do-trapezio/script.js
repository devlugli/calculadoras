function calcular() { const baseMaior = parseFloat(document.getElementById("baseMaior").value) || 0; const baseMenor = parseFloat(document.getElementById("baseMenor").value) || 0; const altura = parseFloat(document.getElementById("altura").value) || 0; const area = ((baseMaior + baseMenor) * altura) / 2; document.getElementById("resumoTopoValor").innerText = Number(area).toLocaleString("pt-BR", { maximumFractionDigits: 4 }) + " u²"; document.getElementById("resumoTopoTexto").innerText = `A area do trapezio é ${document.getElementById("resumoTopoValor").innerText}.`; } window.onload = function () { document.querySelectorAll("input").forEach(function (c) { c.addEventListener("input", calcular); }); calcular(); };
